# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~

# Neovim plugin state (managed by lazy.nvim inside container)
neovim/nvim/lazy-lock.json

# Claude Code (machine-local settings and permission allow-list)
.claude/settings.local.json

# Docker dev environment
docker/.env
docker/persist/zsh_history
docker/persist/.gitconfig.local

# npm: keep package.json / package-lock.json out of the repo *root* only
# (this dotfiles tree is not a Node project), but allow them in any
# subdirectory that genuinely is one. node_modules/ stays globally ignored.
/package.json
/package-lock.json
node_modules/
